services:
  # --- TENANT TOOLBOX STACK (Port 6263) ---

  # --- LIVE SITE STACK (Port 6262) ---
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ctdata_api
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app/api
      - ./reporter:/app/reporter
      - ./nyc:/app/nyc
    environment:
      - DATABASE_URL=postgresql://user:password@ctdata_db:5432/ctdata
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GEMINI_KEY=${GEMINI_KEY}
      - SERPAPI_API_KEY=${SERPAPI_API_KEY}
      - TOOLBOX_ENABLED=false
      - EVICTION_DATA_URL=${EVICTION_DATA_URL}
      - NYC_HPD_ENABLED=${NYC_HPD_ENABLED:-false}
      - NYS_SOCRATA_APP_TOKEN=${NYS_SOCRATA_APP_TOKEN}
      - NHPD_USER=${NHPD_USER}
      - NHPD_PASS=${NHPD_PASS}
      - ALERT_EMAIL_TO=${ALERT_EMAIL_TO:-salmunk@gmail.com}
      - ALERT_EMAIL_FROM=${ALERT_EMAIL_FROM:-salmunk@gmail.com}
      - ALERT_EMAIL_PASS=${ALERT_EMAIL_PASS}
    command: python -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/api/health" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 120s
    networks:
      - default

  nginx:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: ctdata_nginx
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "6262:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - .:/app_root:ro
    networks:
      - default

  updater:
    build:
      context: .
      dockerfile: Dockerfile.updater
    container_name: ctdata_updater
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgresql://user:password@ctdata_db:5432/ctdata
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GEMINI_KEY=${GEMINI_KEY}
      - SERPAPI_API_KEY=${SERPAPI_API_KEY}
      - EVICTION_DATA_URL=${EVICTION_DATA_URL}
      - NYC_HPD_ENABLED=${NYC_HPD_ENABLED:-false}
      - NYS_SOCRATA_APP_TOKEN=${NYS_SOCRATA_APP_TOKEN}
      - NHPD_USER=${NHPD_USER}
      - NHPD_PASS=${NHPD_PASS}
      - ALERT_EMAIL_TO=${ALERT_EMAIL_TO:-salmunk@gmail.com}
      - ALERT_EMAIL_FROM=${ALERT_EMAIL_FROM:-salmunk@gmail.com}
      - ALERT_EMAIL_PASS=${ALERT_EMAIL_PASS}
    networks:
      - default

  geocoder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ctdata_geocoder
    restart: unless-stopped
    depends_on:
      ctdata_db:
        condition: service_healthy
    volumes:
      - ./api:/app/api
    environment:
      - DATABASE_URL=postgresql://user:password@ctdata_db:5432/ctdata
      - DEFAULT_STATE=CT
    command: python -m api.background_geocoder_v2
    networks:
      - default

  ctdata_db:
    image: postgres:15-alpine
    container_name: ctdata_db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=ctdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - default
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d ctdata" ]
      interval: 10s
      timeout: 5s
      retries: 5
    shm_size: 1gb

networks:
  default:
    name: theyownwhat_default
    external: true

volumes:
  postgres_data:

